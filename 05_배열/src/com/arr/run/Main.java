package com.arr.run;
import com.arr.controller.ArrayTestController;
public class Main {
   public static void main(String[] args) {
    //  new ArrayTestController().basicArray();
	  // new ArrayTestController().arrayCopyTest();
	   //new ArrayTestController(). doubleArray()
	   new ArrayTestController().extraTest();
	   ;
   }
}